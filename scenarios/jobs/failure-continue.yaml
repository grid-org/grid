target:
  scope: all
strategy: continue
tasks:
  - backend: test
    action: fail
    params:
      message: "intentional failure"
  - backend: ping
    action: echo
    params:
      message: "continued past failure"
